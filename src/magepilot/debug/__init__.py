"""Autonomous debugging (docs/architecture/08, Phase 4): stack trace → locate →
root cause → fix → validate. The deterministic half lives here — trace parsing and
log reading; the reasoning half is the debug plan template + the graph tools."""
